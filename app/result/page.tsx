"use client";

import { useSearchParams, useRouter } from "next/navigation";
import React from "react";

export default function ResultPage() {
  const searchParams = useSearchParams();
  const router = useRouter();

  const roadmap = searchParams.get("roadmap") || "";
  const skills = searchParams.get("skills") || "";
  const budget = searchParams.get("budget") || "";
  const prompt = searchParams.get("prompt") || "";

  return (
    <div className="relative min-h-screen bg-[#0b0f19] text-white overflow-hidden">

      {/* Background glow */}
      <div className="absolute inset-0 bg-[radial-gradient(circle_at_15%_25%,rgba(59,130,246,0.18),transparent_40%),radial-gradient(circle_at_85%_75%,rgba(168,85,247,0.18),transparent_40%)]"></div>

      {/* Navbar */}
      <div className="sticky top-0 z-20 backdrop-blur-md bg-white/5 border-b border-white/10 px-8 py-5 flex items-center justify-between">
        <h1 className="text-lg font-semibold tracking-wide">
          AI Startup Manager
        </h1>

        <button
          onClick={() => router.back()}
          className="bg-white/10 hover:bg-white/20 transition px-4 py-2 rounded-lg text-sm"
        >
          ← Back
        </button>
      </div>

      {/* Main Content */}
      <div className="relative p-8 md:p-12">

        {/* PROMPT CARD */}
        <div className="mb-10 bg-white/5 border border-white/10 backdrop-blur-2xl rounded-3xl p-6 shadow-xl">
          <h2 className="text-sm uppercase tracking-wider text-gray-400 mb-2">
            Your Startup Idea
          </h2>

          <p className="text-lg font-medium text-blue-300 leading-relaxed whitespace-pre-line">
            {prompt}
          </p>
        </div>

        {/* RESULT GRID */}
        <div className="grid lg:grid-cols-3 gap-8">

          {/* ROADMAP */}
          <div className="bg-white/5 border border-white/10 backdrop-blur-2xl rounded-3xl p-6 shadow-xl hover:shadow-blue-500/10 transition">
            <h2 className="text-xl font-semibold mb-4 text-blue-400">
              🚀 Startup Roadmap
            </h2>

            <div className="whitespace-pre-line text-sm leading-relaxed max-h-[70vh] overflow-y-auto pr-2">
              {roadmap}
            </div>
          </div>

          {/* SKILLS */}
          <div className="bg-white/5 border border-white/10 backdrop-blur-2xl rounded-3xl p-6 shadow-xl hover:shadow-purple-500/10 transition">
            <h2 className="text-xl font-semibold mb-4 text-purple-400">
              🧠 Required Skills
            </h2>

            <div className="whitespace-pre-line text-sm leading-relaxed max-h-[70vh] overflow-y-auto pr-2">
              {skills}
            </div>
          </div>

          {/* BUDGET */}
          <div className="bg-white/5 border border-white/10 backdrop-blur-2xl rounded-3xl p-6 shadow-xl hover:shadow-green-500/10 transition flex flex-col justify-between">
            <div>
              <h2 className="text-xl font-semibold mb-4 text-green-400">
                💰 Time & Budget Prediction
              </h2>

              <div className="whitespace-pre-line text-sm leading-relaxed">
                {budget}
              </div>
            </div>

            <div className="mt-8 pt-6 border-t border-white/10 text-xs opacity-60">
              Generated by AI Startup Planner
            </div>
          </div>

        </div>
      </div>
    </div>
  );
}